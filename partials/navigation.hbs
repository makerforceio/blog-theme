<nav class="navigation">
	<div class="brand">
		{{#if @blog.logo}}
		<a href="{{@blog.url}}"><img class="logo" src="{{@blog.logo}}" alt="{{@blog.title}}" height="48" width="48" /></a>
		{{else}}
		<a href="{{@blog.url}}"><img class="logo" src="{{asset "/images/mark.svg"}}" alt="{{@blog.title}}" height="48" width="48" /></a>
		{{/if}}
		<!-- for testing <span class="name">{{@blog.title}}</span> -->
	</div>
	{{#if @blog.navigation}}
	<ul class="links">
		{{#foreach navigation}}
		<li class="link navigation-{{slug}} {{#if current}}current{{/if}}"><a href="{{url absolute="true"}}">{{label}}</a></li>
		{{/foreach}}
	</ul>
	{{/if}}
</nav>
